# Системные промпты -- управление поведением модели

## Содержание

- [Что такое системный промпт](#что-такое-системный-промпт)
- [Где задается системный промпт](#где-задается-системный-промпт)
- [Компоненты системного промпта](#компоненты-системного-промпта)
- [Структура хорошего промпта](#структура-хорошего-промпта)
- [Примеры для разных задач](#примеры-для-разных-задач)
- [Anti-patterns](#anti-patterns)
- [Мультиязычность](#мультиязычность)
- [Длина и влияние на производительность](#длина-и-влияние-на-производительность)
- [Тестирование и итерации](#тестирование-и-итерации)
- [Практика с llama-server](#практика-с-llama-server)

---

## Что такое системный промпт

Системный промпт -- инструкция, определяющая роль и поведение языковой модели
на протяжении всей сессии. Это первое сообщение в диалоге, невидимое
конечному пользователю, но определяющее характер всех ответов.

```
+----------------------------------+
| System prompt                    |  <- Определяет поведение
| "Ты -- опытный SQL-эксперт..."  |
+----------------------------------+
| User: "Как сделать JOIN?"        |  <- Вопрос пользователя
+----------------------------------+
| Assistant: "JOIN в SQL..."       |  <- Ответ в рамках роли
+----------------------------------+
```

Аналогия: системный промпт -- это должностная инструкция для модели.
Без него модель отвечает "от себя", в общем стиле. С промптом -- в рамках
заданной роли, стиля и ограничений.

### Ключевые свойства

- **Приоритет**: системный промпт имеет наивысший приоритет -- модель следует
  ему, даже если пользователь просит нарушить инструкции
- **Персистентность**: действует на протяжении всей сессии
- **Невидимость**: пользователь не видит системный промпт (но он занимает
  место в контексте)
- **Управляемость**: задается разработчиком, а не пользователем


## Где задается системный промпт

### В chat template

Модели с instruct-тюнингом используют chat template -- специальный формат
разметки сообщений. Системный промпт -- первое сообщение с ролью `system`.

Формат ChatML (Qwen, многие модели):

```
<|im_start|>system
Ты -- опытный SQL-эксперт. Отвечай только на вопросы о SQL.
<|im_end|>
<|im_start|>user
Как сделать JOIN двух таблиц?
<|im_end|>
<|im_start|>assistant
```

Формат Llama 3:

```
<|begin_of_text|><|start_header_id|>system<|end_header_id|>

Ты -- опытный SQL-эксперт. Отвечай только на вопросы о SQL.<|eot_id|>
<|start_header_id|>user<|end_header_id|>

Как сделать JOIN двух таблиц?<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>
```

### Через API (OpenAI-совместимый формат)

llama-server поддерживает OpenAI-совместимый API. Системный промпт передается
как первое сообщение с ролью `system`:

```python
import requests

# Конфигурация запроса к llama-server
response = requests.post(
    "http://localhost:8080/v1/chat/completions",
    json={
        "messages": [
            {
                "role": "system",
                "content": "Ты -- опытный SQL-эксперт. Отвечай кратко."
            },
            {
                "role": "user",
                "content": "Как сделать JOIN двух таблиц?"
            }
        ],
        "temperature": 0.7,
        "max_tokens": 1024
    }
)

result = response.json()
print(result["choices"][0]["message"]["content"])
```

### При запуске llama-server

Системный промпт можно задать глобально при старте сервера:

```bash
llama-server \
    --model ./models/qwen2.5-32b-q4_k_m.gguf \
    --system-prompt "Ты -- технический ассистент. Отвечай по-русски." \
    --host 0.0.0.0 \
    --port 8080
```

Или через файл:

```bash
llama-server \
    --model ./models/qwen2.5-32b-q4_k_m.gguf \
    --system-prompt-file ./prompts/system.txt \
    --host 0.0.0.0 \
    --port 8080
```


## Компоненты системного промпта

Хороший системный промпт состоит из нескольких блоков. Не все блоки
обязательны -- выбирай по задаче.

### 1. Роль

Определяет, кем является модель. Это самый важный компонент.

```
Ты -- старший backend-разработчик с 10-летним опытом работы с Python и PostgreSQL.
```

Без роли модель отвечает обобщенно. С ролью -- как специалист.

### 2. Контекст

Описание ситуации, в которой работает модель.

```
Ты работаешь в стартапе, разрабатывающем систему управления складом.
Стек: Python 3.12, FastAPI, PostgreSQL 16, Redis, Docker.
Код должен быть production-ready.
```

### 3. Инструкции

Конкретные указания по поведению.

```
Инструкции:
- Всегда добавляй type hints к функциям
- Используй async/await для работы с базой данных
- Пиши docstring для каждой функции
- Обрабатывай ошибки явно, не используй bare except
```

### 4. Формат вывода

Как должен выглядеть ответ.

```
Формат ответа:
1. Краткое объяснение подхода (2-3 предложения)
2. Код с комментариями
3. Пример использования
4. Возможные проблемы и как их избежать
```

### 5. Ограничения

Что модель НЕ должна делать.

```
Ограничения:
- Не используй устаревшие библиотеки (requests вместо httpx для async)
- Не генерируй код длиннее 100 строк без разбиения на функции
- Не используй глобальные переменные
- Если не уверен в ответе -- скажи об этом прямо
```

### 6. Примеры (опционально)

Демонстрация ожидаемого поведения.

```
Пример взаимодействия:
Пользователь: "Как подключиться к PostgreSQL?"
Ответ:
  Для подключения используем asyncpg:
  ```python
  import asyncpg
  conn = await asyncpg.connect("postgresql://user:pass@localhost/db")
  ```
```


## Структура хорошего промпта

### Шаблон

```
[РОЛЬ]
Краткое определение роли модели.

[КОНТЕКСТ]
Описание рабочей среды, стека технологий, проекта.

[ИНСТРУКЦИИ]
Пронумерованный список конкретных указаний.

[ФОРМАТ]
Описание структуры ответа.

[ОГРАНИЧЕНИЯ]
Список того, чего следует избегать.
```

### Принципы

1. **Конкретность**: "Отвечай кратко" хуже, чем "Ответ -- не более 3 абзацев"
2. **Однозначность**: каждая инструкция допускает одно толкование
3. **Приоритизация**: самое важное -- в начале промпта
4. **Тестируемость**: можно проверить, следует ли модель инструкции
5. **Компактность**: каждое слово должно нести смысл

### Длина промпта

```
Короткий (50-150 токенов):
  Достаточно для простых ролей. "Ты -- переводчик с английского на русский."

Средний (150-500 токенов):
  Роль + инструкции + формат. Основной рабочий вариант.

Длинный (500-2000 токенов):
  Сложные роли с множеством правил. Агенты, code review, анализ.

Очень длинный (2000+ токенов):
  Избегать. Модель теряет фокус, инструкции начинают противоречить друг другу.
```


## Примеры для разных задач

### Ассистент-переводчик

```
Ты -- профессиональный переводчик с английского на русский.

Правила:
1. Переводи текст максимально близко к оригиналу, сохраняя стиль и тон
2. Технические термины оставляй на английском, если нет устоявшегося
   русского аналога (API, backend, framework)
3. Если термин имеет русский аналог -- используй его: database -> база данных,
   query -> запрос, deploy -> развертывание
4. Не добавляй пояснений к переводу, если пользователь не просит
5. Если текст содержит код -- код не переводи, переводи только комментарии

Формат: только перевод, без вводных фраз.
```

### Технический писатель

```
Ты -- технический писатель, создающий документацию для Python-проектов.

Контекст:
Целевая аудитория -- разработчики с 1-3 годами опыта. Стиль -- сухой,
технический, без воды и маркетинговых фраз.

Инструкции:
1. Структурируй текст: заголовки, подзаголовки, списки
2. Каждый раздел начинай с краткого объяснения "что это и зачем"
3. Добавляй примеры кода с комментариями
4. Указывай версии библиотек, если это важно
5. В конце раздела -- "Подводные камни" (частые ошибки)

Формат:
- Markdown
- Заголовки: ## для разделов, ### для подразделов
- Код: ```python с комментариями на русском
- Списки: нумерованные для последовательностей, маркированные для перечислений

Ограничения:
- Не используй слова: "просто", "легко", "всего лишь"
- Не пиши "в данной статье мы рассмотрим" -- сразу к делу
- Не дублируй информацию
```

### SQL-эксперт

```
Ты -- DBA с 15-летним опытом работы с PostgreSQL.

Контекст:
PostgreSQL 16, таблицы с миллионами строк, высоконагруженная система.

Инструкции:
1. Пиши оптимальные SQL-запросы, учитывая индексы и план выполнения
2. Объясняй выбор подхода: почему CTE, а не подзапрос; почему LEFT JOIN, а не EXISTS
3. Предлагай индексы, если они улучшат производительность
4. Всегда учитывай NULL-значения и edge cases
5. Для сложных запросов показывай EXPLAIN ANALYZE

Формат:
1. SQL-запрос с комментариями
2. Объяснение логики (2-3 предложения)
3. Рекомендации по индексам (если применимо)
4. Альтернативный подход (если есть)

Ограничения:
- Не используй SELECT * -- всегда перечисляй колонки
- Не предлагай хранимые процедуры без крайней необходимости
- Не используй нестандартный синтаксис без пометки
```

### Data Scientist

```
Ты -- data scientist, специализирующийся на анализе данных и машинном обучении.

Контекст:
Стек: Python 3.12, pandas, scikit-learn, matplotlib, numpy.
Данные: табличные, до 10 млн строк, CSV/Parquet.

Инструкции:
1. Начинай с EDA: типы данных, пропуски, распределения, корреляции
2. Предлагай подходящие модели, объясняя выбор
3. Всегда делай train/test split перед обучением
4. Указывай метрики качества и их интерпретацию
5. Код должен быть воспроизводимым: random_state, версии библиотек

Формат:
1. Анализ задачи (что предсказываем, какие данные)
2. Код с пояснениями
3. Интерпретация результатов
4. Рекомендации по улучшению

Ограничения:
- Не используй deep learning для табличных данных без обоснования
- Не игнорируй data leakage
- Не оптимизируй accuracy без учета класс-дисбаланса
```

### Юрист (анализ документов)

```
Ты -- юрист-консультант, специализирующийся на анализе договоров и
нормативных документов.

Инструкции:
1. Анализируй текст договора на предмет рисков для заказчика
2. Выделяй неоднозначные формулировки, требующие уточнения
3. Указывай на отсутствие стандартных пунктов (форс-мажор, разрешение споров)
4. Ссылайся на применимые статьи ГК РФ, если релевантно
5. Предлагай конкретные формулировки для исправления

Формат:
- Пункт договора (цитата)
- Риск / проблема
- Рекомендация
- Предлагаемая формулировка (если нужна замена)

Ограничения:
- Указывай, что это анализ, а не юридическая консультация
- Не давай однозначных прогнозов исхода судебных дел
- При неопределенности -- отмечай необходимость консультации с профильным юристом
```

### Учитель (объяснение тем)

```
Ты -- преподаватель Computer Science. Объясняешь сложные темы простым языком.

Целевая аудитория: студент 2-го курса, знает Python на базовом уровне,
знаком с основами алгоритмов.

Инструкции:
1. Начинай с аналогии из реальной жизни
2. Затем -- формальное определение
3. Пример с кодом на Python
4. Визуализация (ASCII-схема или пошаговое выполнение)
5. Задача для самостоятельного решения

Формат:
## Тема
### Аналогия
### Определение
### Пример
### Задача

Ограничения:
- Не используй сложную терминологию без объяснения
- Не пропускай шаги в рассуждениях
- Один концепт за раз -- не смешивай темы
```


## Anti-patterns

### 1. Слишком длинный промпт

```
ПЛОХО:
  2000+ слов промпта с описанием каждого возможного сценария.
  Модель теряет фокус, начинает игнорировать поздние инструкции.

РЕШЕНИЕ:
  Сокращай до 200-500 токенов. Убирай очевидное.
  Детали выноси в few-shot примеры.
```

### 2. Противоречивые инструкции

```
ПЛОХО:
  "Отвечай кратко, в 1-2 предложения."
  "Всегда приводи 3 примера с подробным объяснением."

РЕШЕНИЕ:
  Перечитай промпт целиком. Проверь, что инструкции совместимы.
  Приоритизируй: что важнее -- краткость или полнота?
```

### 3. Расплывчатые указания

```
ПЛОХО:
  "Будь полезным и отвечай хорошо."
  "Пиши качественный код."

РЕШЕНИЕ:
  "Ответ -- не более 5 предложений."
  "Код должен содержать type hints, docstring и обработку ошибок."
```

### 4. Дублирование стандартного поведения

```
ПЛОХО:
  "Будь вежливым." (модели и так вежливы)
  "Не генерируй опасный контент." (встроенные ограничения)

РЕШЕНИЕ:
  Описывай только специфичное для задачи поведение.
```

### 5. Промпт на другом языке, чем ответы

```
ПЛОХО:
  System (английский): "You are a helpful assistant."
  User (русский): "Объясни SQL JOIN."
  -> Модель может ответить на английском

РЕШЕНИЕ:
  "Ты -- ассистент. Всегда отвечай на русском языке."
  Или добавь: "Язык ответов: русский."
```

### 6. Негативные инструкции без позитивной альтернативы

```
ПЛОХО:
  "Не используй сложные слова."
  "Не пиши длинные предложения."

РЕШЕНИЕ:
  "Используй лексику уровня B1."
  "Предложения -- не длиннее 15 слов."
```

### 7. Игнорирование формата вывода

```
ПЛОХО:
  Промпт описывает роль, но не формат ответа.
  Модель отвечает в произвольном формате, что неудобно для парсинга.

РЕШЕНИЕ:
  Всегда указывай формат, если ответ будет обрабатываться программой.
  "Ответ -- JSON: {\"answer\": \"...\", \"confidence\": 0.0-1.0}"
```


## Мультиязычность

### Указание языка ответа

Для локальных моделей важно явно указывать язык ответа, особенно если
промпт и вопрос на разных языках.

```
Вариант 1 (в роли):
  "Ты -- ассистент. Отвечай всегда на русском языке."

Вариант 2 (отдельная инструкция):
  "Язык ответов: русский. Независимо от языка вопроса, отвечай по-русски."

Вариант 3 (для конкретного формата):
  "Код и команды -- на английском. Объяснения -- на русском."
```

### Промпт на языке ответа

Если нужны ответы на русском -- пиши промпт на русском. Это снижает
вероятность переключения языка:

```
ЛУЧШЕ:
  "Ты -- эксперт по Python. Отвечай кратко, по-русски."

ХУЖЕ:
  "You are a Python expert. Answer in Russian."
```

### Смешанный контент

Для задач, где нужен и русский, и английский:

```
"Ты -- разработчик. Комментарии в коде, имена переменных, SQL-запросы --
на английском. Объяснения, документация, ответы на вопросы -- на русском.
Технические термины без устоявшегося перевода оставляй на английском."
```


## Длина и влияние на производительность

### Системный промпт занимает контекст

Системный промпт -- это обычные токены в начале контекста. Они:
- Занимают место в context window
- Увеличивают время обработки первого запроса (prompt processing)
- НЕ влияют на скорость генерации (token generation)

```
Context window: 8192 токенов
System prompt:  500 токенов
Доступно для диалога: 7692 токенов

Для длинных сессий это критично:
  System (500) + History (5000) + User (500) + Answer (?) = 8192
  На ответ остается ~1692 токена
```

### Оптимизация длины

```
Метод 1: Вынести примеры из system prompt в few-shot
  System: "Ты -- переводчик." (короткий)
  User: "Пример: hello -> привет. Переведи: world"
  -> Примеры в user-сообщении можно менять динамически

Метод 2: Иерархия инструкций
  Основные правила -> в system prompt (всегда)
  Специфичные для задачи -> в user message (по необходимости)

Метод 3: Ссылки вместо описаний
  "Формат: JSON по схеме из user-сообщения"
  -> Схему передаем в user message, где она может меняться
```


## Тестирование и итерации

### Методология

1. **Baseline**: запусти модель без системного промпта, оцени ответы
2. **Первая версия**: напиши промпт с основными компонентами
3. **Тестовые запросы**: подготовь 10-20 типичных запросов
4. **Оценка**: проверь ответы на соответствие ожиданиям
5. **Итерация**: корректируй промпт на основе результатов

### Чек-лист для оценки

```
[ ] Модель следует заданной роли
[ ] Формат ответа соответствует инструкциям
[ ] Язык ответа правильный
[ ] Ограничения соблюдаются
[ ] Ответы не содержат "галлюцинаций"
[ ] Модель корректно обрабатывает edge cases
[ ] Промпт работает с разными моделями (если нужна переносимость)
```

### Пример итерации

```
Версия 1: "Ты -- SQL-эксперт."
  Проблема: слишком общие ответы, нет примеров кода

Версия 2: "Ты -- SQL-эксперт. Всегда показывай запрос и объясняй."
  Проблема: ответы слишком длинные

Версия 3: "Ты -- SQL-эксперт (PostgreSQL 16). Формат: запрос с
  комментариями (до 20 строк), затем объяснение (до 3 предложений)."
  Результат: короткие, конкретные ответы с кодом
```

### A/B тестирование промптов

```python
import requests
import json

# Два варианта системного промпта
prompts = {
    "v1": "Ты -- SQL-эксперт. Отвечай кратко.",
    "v2": "Ты -- DBA PostgreSQL 16. Формат: SQL-запрос + объяснение (до 3 предложений)."
}

# Тестовые вопросы
questions = [
    "Как сделать JOIN двух таблиц?",
    "Как оптимизировать запрос с подзапросом?",
    "Как создать индекс для полнотекстового поиска?",
]

results = {}

for name, system_prompt in prompts.items():
    results[name] = []
    for question in questions:
        response = requests.post(
            "http://localhost:8080/v1/chat/completions",
            json={
                "messages": [
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": question}
                ],
                "temperature": 0.3,  # Низкая температура для воспроизводимости
                "max_tokens": 512
            }
        )
        answer = response.json()["choices"][0]["message"]["content"]
        results[name].append({
            "question": question,
            "answer": answer,
            "tokens": response.json()["usage"]["completion_tokens"]
        })

# Сравнение длины ответов
for name in prompts:
    avg_tokens = sum(r["tokens"] for r in results[name]) / len(results[name])
    print(f"{name}: средняя длина ответа = {avg_tokens:.0f} токенов")
```


## Практика с llama-server

### Запуск с системным промптом

```bash
# Файл prompts/sql-expert.txt
# содержит системный промпт

llama-server \
    --model ./models/qwen2.5-32b-q4_k_m.gguf \
    --system-prompt-file ./prompts/sql-expert.txt \
    --host 0.0.0.0 \
    --port 8080 \
    --ctx-size 8192 \
    --n-gpu-layers 999
```

### Python-обертка для работы с промптами

```python
import requests
from dataclasses import dataclass
from typing import Optional


@dataclass
class LLMConfig:
    """Конфигурация подключения к llama-server."""
    base_url: str = "http://localhost:8080"
    temperature: float = 0.7
    max_tokens: int = 1024


class PromptedLLM:
    """Обертка для работы с llama-server и системными промптами."""

    def __init__(self, config: LLMConfig, system_prompt: str):
        self.config = config
        self.system_prompt = system_prompt
        self.history: list[dict] = []

    def ask(self, question: str, keep_history: bool = True) -> str:
        """Отправить вопрос модели с учетом системного промпта и истории."""
        messages = [{"role": "system", "content": self.system_prompt}]
        messages.extend(self.history)
        messages.append({"role": "user", "content": question})

        response = requests.post(
            f"{self.config.base_url}/v1/chat/completions",
            json={
                "messages": messages,
                "temperature": self.config.temperature,
                "max_tokens": self.config.max_tokens
            }
        )
        response.raise_for_status()

        answer = response.json()["choices"][0]["message"]["content"]

        if keep_history:
            self.history.append({"role": "user", "content": question})
            self.history.append({"role": "assistant", "content": answer})

        return answer

    def reset_history(self):
        """Очистить историю диалога."""
        self.history = []

    @classmethod
    def from_file(cls, config: LLMConfig, prompt_path: str) -> "PromptedLLM":
        """Создать экземпляр, загрузив промпт из файла."""
        with open(prompt_path, "r", encoding="utf-8") as f:
            system_prompt = f.read()
        return cls(config, system_prompt)


# Пример использования
if __name__ == "__main__":
    config = LLMConfig(base_url="http://localhost:8080")

    # SQL-эксперт
    sql_expert = PromptedLLM(
        config,
        system_prompt=(
            "Ты -- DBA PostgreSQL 16. "
            "Формат: SQL-запрос с комментариями, "
            "затем объяснение (до 3 предложений). "
            "Язык: русский."
        )
    )

    answer = sql_expert.ask("Как создать составной индекс?")
    print(answer)

    # Переводчик
    translator = PromptedLLM(
        config,
        system_prompt=(
            "Ты -- переводчик EN->RU. "
            "Формат: только перевод, без пояснений. "
            "Технические термины без устоявшегося перевода "
            "оставляй на английском."
        )
    )

    answer = translator.ask("The quick brown fox jumps over the lazy dog.")
    print(answer)
```

### Хранение промптов

Рекомендуемая структура:

```
prompts/
  system/
    sql-expert.txt
    translator.txt
    code-reviewer.txt
    data-scientist.txt
  templates/
    rag-context.txt      # Шаблон для RAG (с плейсхолдерами)
    function-call.txt    # Шаблон для function calling
```

Промпт-файл -- простой текст без разметки:

```
# prompts/system/sql-expert.txt

Ты -- DBA PostgreSQL 16 с 15-летним опытом.

Инструкции:
1. Пиши оптимальные SQL-запросы
2. Объясняй выбор подхода
3. Предлагай индексы при необходимости

Формат:
- SQL-запрос с комментариями
- Объяснение (до 3 предложений)

Язык: русский.
```


## Дополнительные ресурсы

- [Prompt engineering](./prompt-engineering.md) -- техники составления
  пользовательских промптов
- [Function calling](./function-calling.md) -- специальные системные промпты
  для tool use
- [RAG](./rag/README.md) -- системные промпты для retrieval-augmented generation
- [Примеры промптов для кода](../coding/prompts.md)

## Связанные статьи

- [Prompt engineering](prompt-engineering.md)
- [Function calling](function-calling.md)
- [Промпты для кодинга](../coding/prompts.md)
